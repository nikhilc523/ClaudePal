import ClaudePalKit
import Foundation

#if canImport(ActivityKit)
import ActivityKit

@MainActor
final class LiveActivityController {
    // TODO: Wire to DataProvider for Live Activity updates
    // Live Activity shows current session state (active/waiting/idle)
    // and pending approval count.
}
#endif
